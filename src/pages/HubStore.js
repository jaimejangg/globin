import React, { Component } from 'react';
import './CSS/hubStore.css';
import { Link } from 'react-router-dom';

class HubStore extends Component {
    render() {
        return (
            <Link exact to='/hubstore' >
            </Link>
        )
    }
}

export default HubStore;